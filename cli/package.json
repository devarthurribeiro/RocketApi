{
  "name": "rocketapi-cli",
  "version": "0.0.1",
  "description": "Javascript CLI for RocketApi",
  "main": "dist/main.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpileOnly main.ts",
    "build": "tsc",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^13.9.3",
    "ts-node-dev": "^1.0.0-pre.44",
    "typescript": "^3.8.3"
  }
}
